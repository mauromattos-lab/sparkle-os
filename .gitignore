

# Environment & Secrets
.env
.env.local
.env.*.local
.env.*
!.env.example
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# Zenya raw exports (n8n workflows/executions — dados operacionais, podem conter dados sensíveis)
docs/zenya/raw/

# AI Assistant configs (machine-specific, not project artifacts)
.antigravity/
.codex/
.cursor/
.gemini/

# SparkleOS additions
# AIOX Runtime — session state, handoffs, decision logs (gitignored)
.aios/
.aiox/handoffs/
.aiox/sessions/
.aiox/decisions/

# Vercel local build artifacts
.vercel/
apps/**/.vercel/

# TypeScript build info
*.tsbuildinfo

# pnpm
.pnpm-store/
.pnpm-debug.log

# Testing coverage
coverage/
.vitest-cache/

# Next.js
.next/
out/

# Coolify / Docker local overrides
docker-compose.override.yml
