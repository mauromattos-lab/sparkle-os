---
story_id: "4.3"
title: "Fila de Decisões de Mauro"
epic: "Epic 4 — Interface de Pilotagem v1"
status: "InProgress"
created_by: "@sm (River)"
created_at: "2026-04-12"
assigned_to: "@dev"
priority: "P2"
depends_on: ["4.1"]
quality_gate: "@architect"
executor: "@dev"
quality_gate_tools: ["code_review", "pattern_validation", "api_contract"]
---

# Story 4.3 — Fila de Decisões de Mauro

## User Story

Como Mauro, quero ver apenas as decisões que realmente precisam de mim — com contexto suficiente para decidir na hora — para que eu não precise procurar o que está esperando e não perca tempo com coisas que os agentes podem resolver sozinhos.

## Status
- [x] Draft
- [x] Ready
- [x] InProgress
- [ ] InReview
- [ ] Done

## Acceptance Criteria

- [x] 1. Painel `/cockpit/decisions` lista todas as decisões pendentes com: título, contexto, quem pediu e quando
- [x] 2. Badge com contagem de decisões pendentes visível na navegação lateral (ex: "Decisões (3)")
- [x] 3. Cada decisão tem as opções disponíveis listadas quando presentes (campo `options` do modelo)
- [x] 4. Painel vazio exibe mensagem "Nenhuma decisão pendente — tudo sob controle" (sem estado de erro)
- [x] 5. Dados reais consumidos de `listPendingDecisions()` de `@sparkle-os/core` — sem mock

## Escopo

**IN (esta story entrega):**
- `packages/cockpit/src/services/decisions.service.ts` — wrapper de `listPendingDecisions()` do core
- `packages/cockpit/src/routes/decisions.ts` — painel HTML + badge
- Atualização do `shell.ts` para mostrar badge de contagem no link "Decisões"
- Testes unitários para `decisions.service.ts` (mock do módulo `@sparkle-os/core`)

**OUT (não inclui):**
- Criação ou resolução de decisões pelo cockpit — cockpit é read-only neste epic
- Notificações push ou alertas de nova decisão — polling 30s do shell é suficiente

## Complexidade
**M (Médio)** — Integração com package interno (`@sparkle-os/core`). Risco de conectividade com DB do core.

## Riscos

- **DB do core usa `postgres` (porta 5432):** O package `@sparkle-os/core` usa `postgres.js` (porta 5432 direta), diferente do Brain que migrou para Supabase REST API. Se a porta 5432 estiver bloqueada na rede do desenvolvedor, `listPendingDecisions()` vai falhar. @dev deve verificar conectividade e considerar migração do core para Supabase client (como foi feito no Brain) — registrar ADR se migrar.
- **Decisões inexistentes:** Sistema pode não ter nenhuma decisão registrada ainda. Painel vazio deve ser tratado graciosamente.

## Dev Notes

### Stack e padrões do projeto
[Source: docs/architecture.md §2.3, packages/core/src/index.ts]

```
Runtime:   Node.js 24 + tsx
Framework: Hono v4 + HTML server-side
Package:   packages/cockpit/ (já existe — Story 4.1)
```

### API disponível — `@sparkle-os/core`
[Source: packages/core/src/decisions/decision-router.ts + decision-store.ts]

```typescript
import { listPendingDecisions } from '@sparkle-os/core';

// Retorna PendingDecision[]
const decisions = await listPendingDecisions();

// Tipo PendingDecision:
// {
//   id: string
//   title: string
//   context: string
//   requestedBy: string
//   options?: DecisionOption[]
//   createdAt: string (ISO)
// }
```

O package `@sparkle-os/core` é um workspace local — importado diretamente, sem HTTP.

### Dependência no package.json do cockpit
```json
{
  "dependencies": {
    "@sparkle-os/core": "workspace:*"
  }
}
```

### Badge no shell
Para mostrar a contagem no link da navegação, o `shell.ts` precisa receber a contagem como parâmetro:
```typescript
// shell.ts — função renderShell(content: string, decisionCount: number)
// Exemplo: <a href="/cockpit/decisions">Decisões${count > 0 ? ` (${count})` : ''}</a>
```

### Estrutura de arquivos esperada
```
packages/cockpit/src/
├── routes/
│   ├── shell.ts          # ← atualizar para aceitar decisionCount
│   ├── overview.ts       # ← pode mostrar "X decisões pendentes" no sumário
│   └── decisions.ts      # ← NOVO nesta story
└── services/
    └── decisions.service.ts  # ← NOVO nesta story
```

## Tasks / Subtasks

- [x] 1. Criar `src/services/decisions.service.ts` — wrapper de `listPendingDecisions()` com error handling (retorna `[]` se core indisponível, com log de warning)
- [x] 2. Criar `src/routes/decisions.ts` — Hono route GET `/cockpit/decisions`, HTML com lista de decisões ou estado vazio
- [x] 3. Adicionar `@sparkle-os/core` como dependência no `packages/cockpit/package.json`
- [x] 4. Registrar a rota no `src/index.ts`: `app.route('/cockpit/decisions', decisionsRouter)`
- [x] 5. Atualizar `src/routes/shell.ts` para receber `decisionCount` como parâmetro e mostrar badge no link "Decisões"
- [x] 6. Atualizar `src/routes/overview.ts` para exibir contagem de decisões pendentes no sumário da overview
- [x] 7. Criar testes unitários para `decisions.service.ts` — mock de `@sparkle-os/core`: cenários: lista com decisões, lista vazia, core lança erro (deve retornar [])
- [ ] 8. Executar Pre-Commit CodeRabbit antes de marcar Done

## File List

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| `packages/cockpit/src/services/decisions.service.ts` | CREATED | Service wrapper de `listPendingDecisions()` com graceful degradation |
| `packages/cockpit/src/services/decisions.service.test.ts` | CREATED | 8 testes unitários (mock de @sparkle-os/core) |
| `packages/cockpit/src/routes/decisions.ts` | CREATED | Hono route GET /cockpit/decisions, HTML server-side |
| `packages/cockpit/src/routes/shell.ts` | MODIFIED | Adicionado parâmetro `decisionCount` e badge `.nav-badge` |
| `packages/cockpit/src/routes/overview.ts` | MODIFIED | Integrado `getDecisionsCount()` + card sumário na visão geral |
| `packages/cockpit/src/index.ts` | MODIFIED | Registrada rota `/cockpit/decisions` |
| `packages/cockpit/package.json` | MODIFIED | Adicionado `@sparkle-os/core: workspace:*` nas dependências |
| `packages/cockpit/vitest.config.ts` | MODIFIED | Adicionado resolve alias para `@sparkle-os/core` → source (sem build) |

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- Primary Type: API (consome @sparkle-os/core) + Frontend (HTML server-side)
- Complexity: Medium

**Specialized Agent Assignment:**
- Primary Agents: @dev (pre-commit), @architect (pattern review + api_contract)

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): `wsl bash -c 'cd /mnt/c/Users/Mauro/Desktop/SparkleOS && ~/.local/bin/coderabbit --prompt-only -t uncommitted'`
- [ ] Pre-PR (@devops): `wsl bash -c 'cd /mnt/c/Users/Mauro/Desktop/SparkleOS && ~/.local/bin/coderabbit --prompt-only --base main'`

**CodeRabbit Focus Areas:**
- Graceful degradation quando core está indisponível (não travar o cockpit)
- Nenhum dado hardcoded — HTML renderizado de dados reais do service
- Tipo `PendingDecision` usado consistentemente (não redefini-lo no cockpit)
- Badge no shell atualizado a cada render (não cacheado incorretamente)

**Self-Healing Configuration:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 min
- Severity Filter: CRITICAL

## Handoff

**Próximo agente:** @po (Pax)
**Próximo comando:** `*validate-story-draft 4.3`

---

## QA Results

*(pendente)*

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-04-12 | @sm (River) | Story criada — Draft |
| 2026-04-12 | @po (Pax) | Validação `*validate-story-draft 4.3` — Score 9.5/10 — **GO** — Status: Draft → Ready |
| 2026-04-12 | @dev (Dex) | Implementação completa — 24/24 testes passando — Status: Ready → InProgress |
