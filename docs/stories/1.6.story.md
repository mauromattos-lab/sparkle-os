---
story_id: "1.6"
title: "Protocolo de Escalação para Mauro"
epic: "Epic 1 — Fundação: Habitat, Processo e Arsenal"
status: "Done"
created_by: "@sm (River)"
created_at: "2026-04-11"
assigned_to: "@dev"
priority: "P2 — NFR7: razão entrega/esforço humano"
depends_on: ["1.1", "1.2", "1.4"]
---

# Story 1.6 — Protocolo de Escalação para Mauro

## User Story
Como sistema SparkleOS, quero critérios claros de quando agir autonomamente vs. escalar para Mauro, para que ele seja acionado apenas quando genuinamente necessário.

## Status
- [x] Draft
- [x] Ready (GO — @po Pax, 2026-04-11)
- [x] InProgress
- [x] InReview
- [x] Done

## Acceptance Criteria
- [x] 1. Critérios de escalação documentados: tipos de decisão que requerem Mauro vs. autonomia total do agente
- [x] 2. Protocolo de como apresentar decisão a Mauro: contexto suficiente, opções claras, recomendação explícita
- [x] 3. Estrutura de fila de decisões pendentes definida e implementada (dado que alimenta Interface de Pilotagem no Epic 4)
- [x] 4. Decisões autônomas documentadas explicitamente no ADR ou no log de contexto — nenhuma decisão importante passa despercebida
- [x] 5. Protocolo testado com pelo menos um caso real durante o Epic 1 (documentado)

## Escopo

**IN (esta story entrega):**
- Documento de critérios de escalação em `docs/sops/sop-escalacao-mauro.md`
- DecisionQueue service em `packages/core` (schema Postgres + 3 endpoints Hono)
- Testes Vitest cobrindo o CRUD da fila
- Caso real de escalação documentado durante Epic 1

**OUT (não inclui):**
- UI da fila de decisões (→ Epic 4, Piloting Interface)
- Notificações automáticas para Mauro (→ futuro, placeholder `priority: urgent`)
- Integração com Slack/email (→ futuro)

## Complexidade
**M (Médio)** — Requer código (DecisionQueue service + endpoints) + documentação (SOP) + caso real.

## Riscos
- "Caso real de Epic 1" pode ser difícil de forçar — pode usar a própria decisão de arquitetura de escalação como exemplo meta
- DecisionQueue é consumida pela Interface de Pilotagem (Epic 4) — modelar bem o schema desde agora para não quebrar contrato

## Technical Context

### Classificação de Decisões

| Tipo | Quem decide | Exemplos |
|------|-------------|---------|
| **Autônoma** | Agente | Escolha de biblioteca, estrutura de código, estratégia de teste |
| **Documentada** | Agente + ADR | Decisão arquitetural, mudança de stack, novo padrão |
| **Recomendação** | Agente propõe, Mauro aprova | Mudança de infraestrutura, novo serviço pago, migração |
| **Escalação obrigatória** | Mauro decide | IP da Zenya, aprovação de épico, contratação de serviço caro |

### Triggers de Escalação Obrigatória
1. Qualquer mudança no IP da Zenya (lore, personalidade, assets visuais)
2. Novo custo mensal > $50 adicionado à infraestrutura
3. Acesso a dados de clientes ativos da Zenya
4. Mudança de arquitetura que impacta múltiplos Épicos
5. Decisão que não tem consenso entre agentes (conflito de recomendações)
6. Qualquer operação destrutiva em produção (drop de tabela, delete em massa)

### Data Model — DecisionQueue
```typescript
interface PendingDecision {
  id: string;
  title: string;
  context: string;        // Por que Mauro precisa decidir
  options: Array<{
    label: string;
    description: string;
    recommendation: boolean; // o agente recomenda esta?
    pros: string[];
    cons: string[];
  }>;
  requestedBy: string;    // agentId
  storyId: string | null;
  priority: 'urgent' | 'normal' | 'low';
  status: 'pending' | 'resolved' | 'deferred';
  resolution: string | null;
  createdAt: string;
  resolvedAt: string | null;
}
```

### API Endpoints (Hono — `packages/core`)
```
GET  /api/decisions/pending     # Lista decisões aguardando Mauro
POST /api/decisions             # Agente registra nova decisão pendente
PATCH /api/decisions/:id        # Mauro resolve (via Piloting Interface no Epic 4)
```

### Schema Postgres
```sql
CREATE TABLE pending_decisions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  title TEXT NOT NULL,
  context TEXT NOT NULL,
  options JSONB NOT NULL DEFAULT '[]',
  requested_by TEXT NOT NULL,
  story_id TEXT,
  priority TEXT NOT NULL DEFAULT 'normal',
  status TEXT NOT NULL DEFAULT 'pending',
  resolution TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  resolved_at TIMESTAMPTZ
);
```

## Definition of Done
- [x] Todos os ACs implementados
- [x] Documento de critérios de escalação em `docs/sops/sop-escalacao-mauro.md`
- [x] `packages/core` com DecisionQueue service e endpoints funcionais
- [x] Testes Vitest cobrindo: criar decisão, listar pendentes, resolver
- [x] Caso real de escalação documentado (pelo menos uma decisão criada e resolvida durante Epic 1)
- [x] `npm run test` passando (22/22) — lint não instalado localmente (comportamento conhecido)
- [x] @qa PASS formal antes de marcar Done

## SOP
**SOP-1.6-A: Protocolo de Escalação para Mauro**
- Localização: `docs/sops/sop-escalacao-mauro.md` (criar nesta story)
- Conteúdo mínimo: (1) mapa de decisão: autônoma vs documentada vs recomendação vs obrigatória; (2) como formatar uma escalação — contexto + opções + recomendação; (3) SLA esperado de resposta de Mauro; (4) o que fazer enquanto aguarda decisão (continuar com o que é possível, documentar blocker no contexto)

## File List
- `packages/core/src/decisions/types.ts` — PendingDecision, DecisionOption, CreateDecisionInput, ResolveDecisionInput
- `packages/core/src/decisions/decision-store.ts` — createDecision, listPendingDecisions, resolveDecision
- `packages/core/src/decisions/decision-router.ts` — Hono router: GET /pending, POST /, PATCH /:id
- `packages/core/src/decisions/decision-store.test.ts` — 5 testes Vitest
- `packages/core/src/db/schema.ts` — pendingDecisions table adicionada
- `packages/core/src/index.ts` — decisionRouter registrado em /api/decisions
- `packages/core/migrations/0003_pending_decisions.sql` — migração SQL
- `docs/sops/sop-escalacao-mauro.md` — SOP-1.6-A completo

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- DecisionQueue service implementado com schema Drizzle + 3 endpoints Hono
- 22 testes passando (5 novos da Story 1.6 + 17 anteriores)
- Caso real documentado no SOP-1.6-A seção 6 (meta-exemplo do próprio protocolo de escalação)
- `priority: 'urgent'` preparado como placeholder para notificações futuras (Epic 4)
- Decisões resolvidas mantidas no histórico (não deletadas)

### Change Log
- 2026-04-11: Story implementada por @dev (Dex) — DecisionQueue service completo

## QA Results

**Gate:** PASS  
**Revisado por:** @qa (Quinn) — 2026-04-11  
**Gate file:** `docs/qa/gates/1.6-protocolo-escalacao-mauro.yml`

Todos os 7 checks passaram. 22/22 testes. SOP completo com caso real documentado.
Decisões resolvidas mantidas no histórico. Índice Postgres cobre query de listagem.

---

## Notas para @dev
- DecisionQueue é dado que a Interface de Pilotagem (Epic 4) consome — modelar bem desde o início
- `priority: 'urgent'` deve ter campo de notificação futura (pode ser placeholder por ora)
- Decisões resolvidas são mantidas no histórico (não deletadas) — são evidência do processo
- O "caso real de Epic 1" pode ser a própria decisão de estrutura de escalação — meta!
