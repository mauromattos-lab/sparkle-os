---
story_id: "2.7"
title: "Isolamento de Dados por Cliente na Zenya"
epic: "Epic 2 — Zenya Integrada"
status: "InReview"
created_by: "@sm (River)"
created_at: "2026-04-11"
assigned_to: "@data-engineer"
priority: "P2 — Após Fase 1 (depende 2.1, 2.3, 2.4)"
depends_on: ["2.1 — Done", "2.3 — Done", "2.4 — Done"]
quality_gate: "@dev (Dex)"
---

# Story 2.7 — Isolamento de Dados por Cliente na Zenya

## User Story
Como Mauro, quero garantir que os dados de conversa de cada cliente atendido pela Zenya sejam isolados por Row-Level Security no Postgres — para que seja impossível, por bug ou erro de query, que dados de um cliente vazem para outro.

## Status
- [x] Draft
- [x] Ready (GO condicional — @po Pax, 2026-04-11 | fix: depends_on adicionado 2.4)
- [x] InProgress
- [x] InReview
- [ ] Done

## Acceptance Criteria

- [x] 1. Tabela `zenya_conversations` criada no Supabase com RLS habilitado — migration Drizzle aplicada
- [x] 2. Policy `zenya_client_isolation` ativa: `USING (isolation_key = current_setting('app.current_client_key', TRUE))` — apenas registros do cliente ativo na sessão são visíveis
- [x] 3. `dataIsolationKey` gerado automaticamente no provisionamento (Story 2.4) — verificar que a coluna `data_isolation_key` em `zenya_clients` é gerada corretamente (UUID único, nunca nulo)
- [x] 4. Teste de isolamento: criar 2 clientes distintos com conversas distintas — query com `app.current_client_key` do cliente A retorna apenas conversas de A; query com key de B retorna apenas conversas de B
- [x] 5. Teste de vazamento negativo: query sem `app.current_client_key` configurado retorna 0 registros (ou erro) — sem acesso sem key
- [x] 6. Documentação em `docs/zenya/ISOLATION-SPEC.md` — explica o modelo de isolamento, como configurar a key em runtime, e o que fazer se a key não estiver configurada

## Escopo

**IN (esta story entrega):**
- Migration Drizzle para `zenya_conversations` com RLS
- Schema Drizzle para `zenya_conversations` em `organs/zenya/src/db/`
- Testes de isolamento (Vitest + Supabase real ou mock de RLS)
- `docs/zenya/ISOLATION-SPEC.md`

**OUT (não inclui):**
- Isolamento de dados no n8n ou Chatwoot (sistemas externos — fora do controle do SparkleOS)
- Criptografia de conteúdo de conversas em repouso (→ avaliação futura)
- Interface para Mauro ver quais dados existem por cliente (→ Epic 4)
- RLS em outras tabelas além de `zenya_conversations` (→ expandir conforme necessidade)

## Complexidade
**M (Médio)** — Implementação de RLS no Postgres é bem definida, mas testar o isolamento requer atenção. A integração com o Zenya Adapter (configurar `app.current_client_key` nas queries) é o ponto mais crítico.

## Riscos
- Supabase pode ter restrições na configuração de `set_config` via pool de conexões — verificar se `current_setting` funciona corretamente com o pooler do Supabase (Session vs. Transaction mode)
- Drizzle ORM pode precisar de adaptação para injetar `set_config` antes de cada query — verificar suporte nativo ou criar middleware

## Dev Notes

### Schema e RLS
[Source: docs/architecture.md §9]
```sql
CREATE TABLE zenya_conversations (
  id                 UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  client_id          UUID NOT NULL REFERENCES zenya_clients(id),
  isolation_key      TEXT NOT NULL,
  chatwoot_conv_id   INTEGER,
  content            JSONB NOT NULL DEFAULT '{}',
  created_at         TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
ALTER TABLE zenya_conversations ENABLE ROW LEVEL SECURITY;
CREATE POLICY zenya_client_isolation ON zenya_conversations
  USING (isolation_key = current_setting('app.current_client_key', TRUE));
```

### Padrão de Uso em Runtime
Para queries em `zenya_conversations`, o código deve:
```typescript
// Antes de qualquer query nesta tabela:
await db.execute(
  sql`SELECT set_config('app.current_client_key', ${isolationKey}, TRUE)`
);
// Agora as queries respeitam o RLS automaticamente
const conversations = await db.select().from(zenyaConversations);
```

### Referência: `dataIsolationKey`
[Source: docs/architecture.md §4 — ZenyaClient]
```typescript
dataIsolationKey: string; // chave usada no RLS — gerada no provisionamento
```
A chave é gerada pelo Core no provisionamento (`POST /nucleus/zenya/clients`, Story 2.4) e armazenada em `zenya_clients.data_isolation_key`. Esta mesma chave é inserida em `zenya_conversations.isolation_key` ao salvar conversas.

### Supabase Connection Mode
[Source: docs/architecture.md §2.2]
- Supabase free tier usa Transaction mode por padrão no pooler
- `set_config` com `is_local=TRUE` funciona por transação — compatível com Transaction mode
- Usar `set_config('app.current_client_key', key, TRUE)` (terceiro param = is_local)

### Padrão Drizzle do packages/core
[Source: packages/core/src/db/ — padrão Epic 1]
- Drizzle com `postgres` driver já em uso no Core
- `organs/zenya/src/db/` deve seguir o mesmo padrão de schema + migrations

## Tasks / Subtasks

- [x] **Task 1** — Schema e Migration (AC: 1, 2)
  - [x] 1.1 Criar schema Drizzle para `zenya_conversations` em `organs/zenya/src/db/schema.ts`
  - [x] 1.2 Gerar migration com Drizzle Kit
  - [x] 1.3 Aplicar migration no Supabase — `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` + `CREATE POLICY` (nota: tabela legada renomeada para `zenya_conversations_legacy`)
  - [x] 1.4 Verificar via Supabase dashboard que RLS está ativo e a policy está criada

- [x] **Task 2** — Validação do `dataIsolationKey` (AC: 3)
  - [x] 2.1 Verificar que `zenya_clients.data_isolation_key` é gerado como UUID único no INSERT — `crypto.randomUUID()` em `clients.ts:63`, DEFAULT `gen_random_uuid()::TEXT` no banco
  - [x] 2.2 Teste: dois clientes provisionados nunca têm a mesma `data_isolation_key` — `isolation.test.ts`

- [x] **Task 3** — Testes de Isolamento (AC: 4, 5)
  - [x] 3.1 Criar teste: INSERT de conversas com 2 isolation_keys distintas
  - [x] 3.2 Teste positivo: query com `set_config` do cliente A retorna apenas conversas de A
  - [x] 3.3 Teste positivo: query com `set_config` do cliente B retorna apenas conversas de B
  - [x] 3.4 Teste negativo: query sem `set_config` retorna 0 registros (RLS bloqueia)
  - [x] 3.5 Teste negativo: query com key inválida retorna 0 registros

- [x] **Task 4** — Documentação (AC: 6)
  - [x] 4.1 Redigir `docs/zenya/ISOLATION-SPEC.md` com: modelo, como configurar key, o que acontece sem key, referência ao schema

- [x] **Task 5** — Qualidade e Entrega
  - [x] 5.1 `npm run typecheck` + `npm run test` + `npm run build` PASS — 38/38 testes
  - [x] 5.2 Notificar @dev para Quality Gate

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Quality Gate do @dev (Dex) — foco em segurança e correção do RLS.

## Definition of Done
- [x] `zenya_conversations` com RLS ativa no Supabase — verificado via Supabase MCP
- [x] Migration Drizzle aplicada sem erros
- [x] Testes de isolamento (positivo + negativo) passando — 9 testes
- [x] `docs/zenya/ISOLATION-SPEC.md` criado
- [x] `npm run typecheck`, `npm run test`, `npm run build` PASS — 38/38 testes
- [x] @dev Quality Gate PASS
- [ ] @devops push realizado

## Handoff
**Próximo agente:** @po (Pax) — `*validate-story-draft 2.7`
**Após Ready:** @data-engineer (Dara) — ativar `@data-engineer` e executar Story 2.7

## Change Log
| Data | Agente | Ação |
|------|--------|------|
| 2026-04-11 | @sm (River) | Story criada — Draft |
| 2026-04-11 | @po (Pax) | Validação 8/10 — GO condicional → Ready | Fix: depends_on adicionado 2.4 (FK constraint zenya_conversations → zenya_clients) |
| 2026-04-11 | @data-engineer (Dara) | Implementação completa — 38/38 testes PASS, migration aplicada no Supabase, tabela legada preservada em zenya_conversations_legacy → InProgress. Aguarda @dev Quality Gate |
| 2026-04-11 | @qa (Quinn) | QA Review — FAIL inicial: set_config sem db.transaction() (bug de isolamento em produção). Fix aplicado: conversations.ts usa db.transaction(). Re-review: 38/38 PASS → PASS → InReview |

## File List
- `organs/zenya/src/db/schema.ts` — atualizado (adicionado zenyaConversations + types)
- `organs/zenya/src/db/conversations.ts` — criado (helper com setIsolationKey, listConversations, insertConversation)
- `organs/zenya/src/db/isolation.test.ts` — criado (9 testes: Task 2 + Task 3)
- `organs/zenya/migrations/0002_zenya_conversations_rls.sql` — criado (migration SQL)
- `docs/zenya/ISOLATION-SPEC.md` — criado
