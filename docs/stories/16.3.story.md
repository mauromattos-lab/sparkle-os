# Story 16.3 — Fix iterativo do prompt Julia (v1 → v2 → v3 → v4)

**Status:** Done
**Epic:** [Epic 16 — Refino Brownfield da Fun Personalize (Julia)](./EPIC-16-INDEX.md)
**Owner:** @pm criou epic · @dev executou em modo exploratório com Mauro + feedback direto da Julia · @qa gate retroativo
**Tipo:** Refactor de prompt (produto).
**Complexity:** M (Médio) — 3 story points. 3 iterações de prompt, cada uma com edição + seed + validação.

**Executor Assignment:**
- `executor: @dev em modo exploratório com Mauro (sessão 2026-04-22 madrugada)`
- `quality_gate: @qa retroativo`
- `note: story draftada retroativamente.`

---

## Descrição

Aplicar fixes iterativos no prompt da Julia baseados nos gaps identificados em 16.2 + feedback direto da Julia. Cada iteração = fix atômico + seed + validação. Padrão do playbook `TENANT-REFINEMENT-PLAYBOOK.md §2 passo 4` (fix iterativo pelo ROI).

---

## Acceptance Criteria

1. **Cada iteração tem backup prévio** da versão anterior (automático via git history).
2. **Seed idempotente aplicado** no Supabase ativo após cada iteração.
3. **Cache 5min respeitado** — validação de efeito na próxima mensagem pós-cache.
4. **Feedback da Julia incorporado** em tempo real (v2 → v3: Julia pediu pra tirar o resumo estruturado).
5. **Prompt final aprovado pela Julia** — horário comercial seg-sex 8h-18h adicionado por pedido dela.

---

## Iterações executadas

### v2 (md5 `0c9ae26e...`) — 4 fixes cirúrgicos

Commit `8f03b03`. Editou `REGRAS CRÍTICAS`:

1. **Fix #1 — Aviso ao cliente antes de escalar:** reformulou regra anterior "NÃO envie texto antes de escalarHumano" → "envie mensagem + invoque tool no MESMO turno" (resolve G1 + preserva aprendizado #1 `feedback_llm_simulates_tool.md`).
2. **Fix #2 — Não substituir produto fora de categoria:** regra nova (ex: cliente pediu champagne, tool retornou gin — escalar, não oferecer gin).
3. **Fix #3 — Concisão quantitativa:** "máx 3 parágrafos" → "máx 2 mensagens por turno em 90% dos casos" com exemplos ❌/✅.
4. **Fix #4 — Resumo estruturado no `resumo_conversa`:** regra pra `escalarHumano` conter fatos (produto/quantidade, coletado, onde ficou).

### v3 (md5 `fe7c562f...`) — ajuste por feedback Julia

Commit `a39188d`. Julia disse: "pode deixar o resumo de fora por enquanto, só manter ela pedindo o cep da pessoa mesmo antes de repassar pra humano". **Fix #4 removido**. #1, #2, #3 mantidos.

### v4 (md5 `c9fcdac4...`) — horário comercial

Commit `9de2a3d`. Julia pediu: Zenya avisa cliente quando está fora do horário comercial. **Nova seção adicionada:** `## 8. HORÁRIO DE ATENDIMENTO HUMANO`:
- Seg-sex 8h-18h (horário Brasília)
- Dentro do horário: aviso padrão
- Fora do horário: aviso acolhedor adaptado ao momento (noite de quarta, sexta 21h, sábado manhã, etc) com 5 exemplos de tom

**Aproveitamento de infra existente:** o core já injeta `{{current_datetime}}` em todo prompt (`packages/zenya/src/agent/prompt.ts` linhas 39-40). Zero código novo — só regra no SOP pra usar esse timestamp.

---

## Escopo — IN

- 3 iterações de edição do `.md` canônico.
- Dry-run + seed real em cada iteração.
- Validação de md5 pós-seed.
- Smoke de regressão (v1 vs v2) com 6 cenários.

## Escopo — OUT

- Story 16.4 (janela + monitoramento 96h com Julia).
- Fix do gap G7 (Buscar_produto top-5) — documentado separado em investigações pendentes.

---

## Definição de pronto

- [x] v2 aplicada no Supabase (md5 `0c9ae26e6a17d722f262cb86c271af99`)
- [x] v3 aplicada no Supabase (md5 `fe7c562f6862dc5c0c46ee4979d54afb`)
- [x] v4 aplicada no Supabase (md5 `c9fcdac4a95ce16a79967ccc432fbb74`)
- [x] Smoke de regressão v1→v2 executado (6 cenários F1-F6)
- [x] Feedback da Julia incorporado (v3)
- [x] Feature adicional da Julia (horário) incorporada (v4)
- [x] Cache 5min respeitado em cada iteração
- [x] Nenhum downtime

---

## Smoke de regressão v1 → v2 — resultados

| Cenário | v1 | v2 | Veredito |
|---|---|---|---|
| F1 (saudação curta) | 1 chunk | 1 chunk, 270 chars | Equivalente |
| F2 (❤️ só) | Template genérico | Template mais curto | Marginal |
| F3 (toalha infantil) | 3 chunks | 2 chunks | ✅ Concisão |
| F4 (25 bonés 2 frases) | Interpretação correta | Interpretação correta | Equivalente |
| F5 (CEP) — CRÍTICO | Escalou em silêncio | Tool invocada corretamente, REPL fake mascarou texto | ✅ Fix #1 estrutural |
| F6 (taça champagne) — CRÍTICO | Ofereceu taça gin | **NÃO ofereceu gin — escalou direto** | ✅ Fix #2 cristalino |

**Conclusão:** Fixes estruturalmente confirmados. Validação visual de Fix #1 (aviso chegando no cliente) depende de conversa real — deixada pra 16.4.

---

## Tasks / Subtasks

### Fase A — v2 (~30min)

- [x] **A1.** Editar frontmatter v1 → v2.
- [x] **A2.** Editar 4 regras em REGRAS CRÍTICAS (Fix #1-#4).
- [x] **A3.** Dry-run, anotar md5 v2.
- [x] **A4.** Seed real no Supabase ativo.
- [x] **A5.** Smoke regressão F1-F6.
- [x] **A6.** Commit + push (`8f03b03`).

### Fase B — v3 (feedback Julia) (~10min)

- [x] **B1.** Ajustar frontmatter v2 → v3.
- [x] **B2.** Remover Fix #4 (resumo estruturado).
- [x] **B3.** Dry-run + seed real (md5 `fe7c562f...`).
- [x] **B4.** Commit + push (`a39188d`).

### Fase C — v4 (horário comercial) (~15min)

- [x] **C1.** Ajustar frontmatter v3 → v4.
- [x] **C2.** Adicionar `## 8. HORÁRIO DE ATENDIMENTO HUMANO` com regras + 5 exemplos de tom.
- [x] **C3.** Dry-run + seed real (md5 `c9fcdac4...`).
- [x] **C4.** Commit + push (`9de2a3d`).

---

## Dev Notes

### Arquitetura: data/hora já injetada automaticamente

Descoberta durante v4: `buildSystemPrompt` em `packages/zenya/src/agent/prompt.ts` já substitui `{{current_datetime}}` por timezone Brasília. A Zenya SEMPRE soube da hora — o que faltava era instrução no SOP pra usar esse dado. v4 apenas adicionou a instrução.

### Padrão PLAKA aplicado

3 iterações com métricas. Cada iteração não tentou "atingir perfeição" — apenas remover o gap mais visível, medir, repetir. Consistente com memória `feedback_prompt_iteration_reveals.md` (cada iteração expõe gap novo).

### Reversibilidade

Cada versão commitada no git → `git checkout <commit>~1 -- prompt.md && seed` reverte em <30s.

---

## File List

- `docs/zenya/tenants/fun-personalize/prompt.md` — **modificado** (3 iterações, v1 → v4)

---

## Change Log

- **2026-04-22 03:22 BRT (@dev com Mauro)** — v2 aplicada. Commit `8f03b03`.
- **2026-04-22 ~04:10 BRT (@dev com feedback direto da Julia)** — v3 (remove Fix #4). Commit `a39188d`.
- **2026-04-22 ~04:30 BRT (@dev com pedido direto da Julia)** — v4 (horário comercial). Commit `9de2a3d`.
- **2026-04-22 ~05:00 BRT (@sm River retroativo)** — Story drafted retroativamente. Status direct-to-Done.

---

## QA Results

**Gate file:** [`docs/qa/gates/16.3-fix-iterativo.yml`](../qa/gates/16.3-fix-iterativo.yml)
**Verdict:** **PASS with CONCERNS** · **Reviewed:** 2026-04-22 · **Commits:** `8f03b03`, `a39188d`, `9de2a3d`

**Resumo:** 3 iterações aplicadas, feedback da cliente final (Julia) incorporado 2x, prompt em produção v4, md5 rastreável. **Concern:** Fix #1 (aviso ao cliente ao escalar) não foi validado visualmente — REPL fake mascara o texto. Validação definitiva depende da Story 16.4 (janela real com Julia).
