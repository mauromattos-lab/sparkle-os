---
story_id: "4.7"
title: "Progresso de Épicos e Stories"
epic: "Epic 4 — Interface de Pilotagem v1"
status: "Ready"
created_by: "@sm (River)"
created_at: "2026-04-12"
assigned_to: "@dev"
priority: "P3"
depends_on: ["4.1"]
quality_gate: "@architect"
executor: "@dev"
quality_gate_tools: ["code_review", "pattern_validation"]
---

# Story 4.7 — Progresso de Épicos e Stories

## User Story

Como Mauro, quero ver o progresso de cada épico em percentual — quantas stories estão Done, em andamento ou bloqueadas — para que eu entenda o avanço real do SparkleOS sem abrir cada arquivo manualmente.

## Status
- [x] Draft
- [x] Ready
- [ ] InProgress
- [ ] InReview
- [ ] Done

## Acceptance Criteria

- [ ] 1. Painel `/cockpit/epics` lista todos os épicos (agrupados por prefixo: `1.x`, `2.x`, `3.x`, `4.x`) com: total de stories, contagem por status e percentual de completude
- [ ] 2. Stories com status `InProgress` destacadas com o agente responsável (`assigned_to`)
- [ ] 3. Stories com status `Draft` ou `Ready` há mais de 7 dias listadas como "aguardando" — sinaliza backlog parado
- [ ] 4. Dados lidos diretamente de `docs/stories/*.story.md` — parsing de frontmatter YAML
- [ ] 5. Painel acessível via navegação lateral do shell

## Escopo

**IN (esta story entrega):**
- `packages/cockpit/src/services/stories-parser.service.ts` — lê e parseia todos os arquivos `*.story.md`
- `packages/cockpit/src/routes/epics.ts` — painel HTML com progresso por épico
- Testes unitários para `stories-parser.service.ts` (mock `fs/promises`)

**OUT (não inclui):**
- Edição de stories pelo cockpit — read-only
- Histórico de velocidade ou gráficos de burndown — texto + cor suficiente para v1
- Stories de arquivos que não seguem o padrão `{n}.{m}.story.md` são ignoradas silenciosamente

## Complexidade
**B (Baixa)** — Parsing de arquivos locais, sem dependências externas. Lógica simples de agrupamento.

## Riscos

- **Reutilização do parser da Story 4.2:** A Story 4.2 (`agent-activity.service.ts`) já implementa parsing básico de frontmatter. `stories-parser.service.ts` deve reutilizar ou extrair essa lógica para evitar duplicação — @dev deve verificar se é melhor extrair para um helper compartilhado (`src/lib/frontmatter.ts`) ou replicar.
- **Arquivos não-story em docs/stories/:** Arquivos como `EPIC-4-INDEX.md`, `epics/` subdiretório, devem ser ignorados. Filtrar apenas arquivos que correspondem ao padrão `/^\d+\.\d+\.story\.md$/`.

## Dev Notes

### Stack e padrões do projeto
[Source: docs/architecture.md §2.3, packages/cockpit/src/ — criado na Story 4.1]

```
Runtime:   Node.js 24 + tsx
Framework: Hono v4 + HTML server-side
Package:   packages/cockpit/ (já existe — Story 4.1)
```

### Fonte de dados: `docs/stories/*.story.md`
[Source: docs/stories/ — estrutura atual do projeto]

```typescript
import { readdir, readFile } from 'fs/promises';
import { join, basename } from 'path';

const STORIES_DIR = join(process.cwd(), 'docs/stories');

// Filtrar apenas arquivos do padrão X.Y.story.md (sem subdiretórios)
const files = (await readdir(STORIES_DIR))
  .filter(f => /^\d+\.\d+\.story\.md$/.test(f));

// Para cada arquivo, extrair frontmatter:
// story_id, title, status, assigned_to, created_at, epic
```

### Parser de frontmatter
Reutilizar a função `parseFrontmatter()` da Story 4.2 se extraída para `src/lib/frontmatter.ts`, ou reimplementar:

```typescript
function parseFrontmatter(content: string): Record<string, string> {
  const match = content.match(/^---\n([\s\S]*?)\n---/);
  if (!match) return {};
  const result: Record<string, string> = {};
  for (const line of match[1].split('\n')) {
    const [key, ...rest] = line.split(':');
    if (key && rest.length) result[key.trim()] = rest.join(':').trim().replace(/^["']|["']$/g, '');
  }
  return result;
}
```

### Agrupamento por épico
```typescript
// story_id: "4.2" → epic: 4
// Agrupar por Math.floor(parseFloat(story_id))
// Ordenar épicos crescente
// Para cada épico: { epicNum, stories: StoryMeta[], doneCount, inProgressCount, totalCount }
```

### Detecção de stories "paradas"
```typescript
// Stories com status Draft ou Ready
// created_at: "2026-04-12" → verificar se mais de 7 dias atrás
const sevenDaysAgo = Date.now() - 7 * 24 * 60 * 60 * 1000;
const isStale = new Date(story.created_at).getTime() < sevenDaysAgo;
```

### Estrutura de arquivos
```
packages/cockpit/src/
├── lib/
│   └── frontmatter.ts           # ← extrair helper (opcional — decisão do @dev)
├── routes/
│   └── epics.ts                 # ← NOVO nesta story
└── services/
    └── stories-parser.service.ts  # ← NOVO nesta story
```

## Tasks / Subtasks

- [ ] 1. Criar `src/services/stories-parser.service.ts` — `parseAllStories()`: lê `docs/stories/*.story.md`, filtra por padrão de nome, parseia frontmatter, retorna `StoryMeta[]`
- [ ] 2. Adicionar `groupByEpic(stories: StoryMeta[])` no mesmo service — agrupa e calcula percentual de completude por épico
- [ ] 3. Adicionar lógica de stories "paradas" (Draft/Ready há mais de 7 dias)
- [ ] 4. Considerar extrair `parseFrontmatter()` para `src/lib/frontmatter.ts` se Story 4.2 não o fez (evitar duplicação)
- [ ] 5. Criar `src/routes/epics.ts` — Hono route GET `/cockpit/epics`, HTML com tabela de épicos + stories InProgress + stories paradas
- [ ] 6. Registrar a rota no `src/index.ts`
- [ ] 7. Criar testes unitários para `stories-parser.service.ts` — mock `fs/promises`: stories normais, arquivo sem frontmatter, arquivo com nome inválido (ignorado)
- [ ] 8. Executar Pre-Commit CodeRabbit antes de marcar Done

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- Primary Type: Frontend (HTML server-side) + Integration (fs — filesystem)
- Complexity: Low

**Specialized Agent Assignment:**
- Primary Agents: @dev (pre-commit), @architect (pattern_validation)

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): `wsl bash -c 'cd /mnt/c/Users/Mauro/Desktop/SparkleOS && ~/.local/bin/coderabbit --prompt-only -t uncommitted'`
- [ ] Pre-PR (@devops): `wsl bash -c 'cd /mnt/c/Users/Mauro/Desktop/SparkleOS && ~/.local/bin/coderabbit --prompt-only --base main'`

**CodeRabbit Focus Areas:**
- Regex de filtro de arquivos: `/^\d+\.\d+\.story\.md$/` — não vazar arquivos não-story
- Sem duplicação de `parseFrontmatter` — verificar Story 4.2
- `try/catch` por arquivo — um arquivo corrompido não quebra o painel inteiro
- Sem path traversal — `readdir` com diretório fixo, sem input do usuário

**Self-Healing Configuration:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 min
- Severity Filter: CRITICAL

## Handoff

**Próximo agente:** @po (Pax)
**Próximo comando:** `*validate-story-draft 4.7`

---

## QA Results

*(pendente)*

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-04-12 | @sm (River) | Story criada — Draft |
| 2026-04-12 | @po (Pax) | Validação `*validate-story-draft 4.7` — Score 9.5/10 — **GO** — Status: Draft → Ready |
