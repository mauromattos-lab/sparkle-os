---
story_id: "1.4"
title: "ADR Registry"
epic: "Epic 1 — Fundação: Habitat, Processo e Arsenal"
status: "Ready"
created_by: "@sm (River)"
created_at: "2026-04-11"
assigned_to: "@dev"
priority: "P1 — FR12 crítico para rastreabilidade arquitetural"
depends_on: ["1.1", "1.2"]
---

# Story 1.4 — ADR Registry

## User Story
Como agente AIOS, quero registrar decisões arquiteturais formalmente, para que todo time de IA atual e futuro saiba o que foi decidido, por quê, e o que foi descartado.

## Status
- [x] Draft
- [x] Ready (GO — @po Pax, 2026-04-11)
- [x] InProgress
- [x] InReview
- [x] Done

## Acceptance Criteria
- [x] 1. Template de ADR definido e documentado em `docs/adrs/ADR-TEMPLATE.md`
- [x] 2. Localização dos ADRs: `docs/adrs/adr-NNN-titulo-kebab-case.md` — acessível a todos os agentes
- [x] 3. Primeira ADR criada: `docs/adrs/adr-001-repository-structure.md` documentando decisão de estrutura monorepo pnpm da Story 1.1
- [x] 4. ADRs indexadas no Postgres — consultáveis por agentes em sessões futuras via API
- [x] 5. SOP de criação de ADR documentado em `docs/sops/sop-criar-adr.md` — inclui regra: toda decisão arquitetural relevante gera um ADR com numeração sequencial provida pela API (`GET /api/adrs/next-number`)

## Escopo

**IN (esta story entrega):**
- Template de ADR em `docs/adrs/ADR-TEMPLATE.md`
- ADR-001 criada: `docs/adrs/adr-001-repository-structure.md` (decisão de monorepo pnpm)
- ADR service em `packages/core` com 4 endpoints (list, get, create, next-number)
- Schema Postgres para indexação de ADRs
- SOP de criação de ADR

**OUT (não inclui):**
- UI para visualizar ADRs (→ Epic 4)
- ADRs de outras decisões (cada story cria sua própria ADR conforme necessário)
- Busca semântica por ADRs (→ Collective Brain, Epic 3)

## Complexidade
**S (Pequeno)** — ADRs são principalmente arquivos Markdown + CRUD simples no Postgres.

## Riscos
- Numeração sequencial requer coordenação entre agentes — o serviço deve ser a fonte de verdade para o próximo número disponível (não deixar à cargo do agente calcular)
- AC5 ("processo integrado ao fluxo") é regra de processo, não entrega técnica — reformulado como SOP obrigatório

## Technical Context

### Data Model — ADR (de `docs/architecture.md` §4)

```typescript
interface ADR {
  id: string;
  number: number;         // sequencial: 001, 002...
  title: string;
  status: 'proposed' | 'accepted' | 'deprecated' | 'superseded';
  context: string;        // Por que a decisão foi necessária
  decision: string;       // O que foi decidido
  rationale: string;      // Por que esta escolha
  alternatives: string[]; // O que foi descartado e por quê
  consequences: string;   // Impactos da decisão
  createdBy: string;      // agentId
  storyId: string | null;
  filePath: string;       // Path do arquivo .md
  createdAt: string;
  updatedAt: string;
}
```

### Template de ADR (conteúdo de `ADR-TEMPLATE.md`)

```markdown
# ADR-NNN — Título da Decisão

**Status:** Proposed | Accepted | Deprecated | Superseded
**Data:** YYYY-MM-DD
**Autor:** @agent-id
**Story:** X.Y (se aplicável)

## Contexto
[Por que esta decisão foi necessária? Qual problema resolve?]

## Decisão
[O que foi decidido, de forma objetiva.]

## Rationale
[Por que esta escolha? Quais os critérios usados?]

## Alternativas Descartadas
| Alternativa | Razão de Descarte |
|-------------|------------------|
| Opção A     | ...              |
| Opção B     | ...              |

## Consequências
[Impactos positivos e negativos desta decisão.]

## Referências
- Link para story, PRD, ou outro artefato relacionado
```

### API Endpoints (Hono — `packages/core`)
```
GET  /api/adrs                # Lista todas as ADRs (ordenadas por número)
GET  /api/adrs/:number        # ADR específica por número
POST /api/adrs                # Registra nova ADR (cria arquivo .md + persiste no Postgres)
GET  /api/adrs/next-number    # Retorna próximo número disponível (fonte de verdade para numeração)
```

### Schema Postgres
```sql
CREATE TABLE adrs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  number INTEGER NOT NULL UNIQUE,
  title TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'proposed',
  context TEXT,
  decision TEXT,
  rationale TEXT,
  alternatives JSONB DEFAULT '[]',
  consequences TEXT,
  created_by TEXT,
  story_id TEXT,
  file_path TEXT NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

## Definition of Done
- [x] Todos os ACs implementados
- [x] `ADR-TEMPLATE.md` criado em `docs/adrs/`
- [x] `adr-001-repository-structure.md` criado documentando decisão da Story 1.1
- [x] Service de ADR no `packages/core` com endpoints funcionais
- [x] Testes Vitest cobrindo: create ADR, list, get by number
- [x] SOP de criação de ADR (ver abaixo)
- [x] `npm run test` e `npm run lint` passando
- [x] @qa PASS formal antes de marcar Done

## SOP
**SOP-1.4-A: Como criar uma ADR no SparkleOS**
- Localização: `docs/sops/sop-criar-adr.md` (criar nesta story)
- Conteúdo mínimo: (1) quando criar ADR (toda decisão arquitetural relevante); (2) como numerar sequencialmente; (3) quem cria (@architect por padrão, outros agentes podem criar); (4) como atualizar status de ADR existente; (5) ADRs nunca são deletadas — apenas marcadas como deprecated/superseded

## File List
**Modificados nesta story:**
- `packages/core/src/db/schema.ts` — tabela `adrs` adicionada ao schema Drizzle
- `packages/core/src/index.ts` — `adrRouter` registrado em `/api/adrs`, exports de ADR adicionados

**Criados nesta story:**
- `packages/core/src/adr/types.ts` — interfaces ADR, CreateAdrInput, AdrStatus
- `packages/core/src/adr/adr-store.ts` — serviço: createAdr, listAdrs, getAdrByNumber, getNextAdrNumber, adrFilePath
- `packages/core/src/adr/adr-router.ts` — Hono router: 4 endpoints /api/adrs
- `packages/core/src/adr/adr-store.test.ts` — 8 testes Vitest (todos passando)
- `packages/core/migrations/0002_adrs.sql` — migration SQL
- `docs/adrs/ADR-TEMPLATE.md` — template padrão de ADR
- `docs/adrs/adr-001-repository-structure.md` — ADR-001: decisão monorepo pnpm
- `docs/sops/sop-criar-adr.md` — SOP-1.4-A: como criar ADR no SparkleOS

## Notas para @dev
- ADR-001 deve ser a primeira criada — documentar a decisão do monorepo pnpm
- Numeração: sempre 3 dígitos com zero-padding (001, 002, ..., 010, 011...)
- Arquivo .md é a fonte de verdade — Postgres é índice para busca rápida por agentes
- Quando `POST /api/adrs` é chamado: (1) cria o arquivo .md em `docs/adrs/`; (2) persiste metadados no Postgres
- ADRs são write-once (exceto `status` e `updatedAt`) — não modificar decisões passadas
