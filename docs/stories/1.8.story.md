---
story_id: "1.8"
title: "Rastreamento de Custo por Operação"
epic: "Epic 1 — Fundação: Habitat, Processo e Arsenal"
status: "Done"
created_by: "@sm (River)"
created_at: "2026-04-11"
assigned_to: "@dev"
priority: "P2 — NFR4: custo visível e rastreável"
depends_on: ["1.1", "1.3"]
---

# Story 1.8 — Rastreamento de Custo por Operação

## User Story
Como Mauro, quero visibilidade do custo de cada tipo de operação dos agentes, para que decisões de uso de ferramentas tenham fundamento financeiro real.

## Status
- [x] Draft
- [x] Ready (GO — @po Pax, 2026-04-11)
- [x] InProgress
- [x] InReview
- [x] Done

## Acceptance Criteria
- [x] 1. Custo por tipo de operação rastreado: geração de texto (Claude API — tokens input/output), embeddings (Voyage AI), pesquisa web (EXA — por query), operações de banco (Supabase — dentro do free tier)
- [x] 2. Custo por agente discriminado — cada operação tagged com `agentId`
- [x] 3. Agregação por período disponível: diário, semanal, mensal
- [x] 4. Dados estruturados e disponíveis via API para a Interface de Pilotagem (Epic 4) — não precisa de UI agora
- [x] 5. SOP de como revisar e otimizar custos quando necessário

## Escopo

**IN (esta story entrega):**
- CostTracking service em `packages/core` (schema Postgres + 4 endpoints Hono)
- Tabela de preços configurável (não hardcoded)
- Agregação por período e por agente
- SOP de revisão de custos (`sop-gestao-custos.md`)
- Testes Vitest cobrindo registrar, agregar, orçamento

**OUT (não inclui):**
- UI de custos (→ Epic 4, Piloting Interface)
- Alertas automáticos em tempo real (→ threshold documentado, implementação futura)
- Captura automática de tokens Claude API (→ integração futura no response handler)
- Billing de clientes (não é o escopo — é custo de infraestrutura de Mauro)

## Complexidade
**M (Médio)** — Requer código (service + endpoints + agregações SQL) + configuração de preços + SOP.

## Riscos
- Fire-and-forget de custo pode silenciosamente falhar — garantir logging de erros
- Preços de modelos mudam — configuração via variável de ambiente é essencial

## Technical Context

### Data Model — CostEvent
```typescript
interface CostEvent {
  id: string;
  agentId: string;
  operationType: 'llm_input' | 'llm_output' | 'embedding' | 'web_search' | 'storage_read' | 'storage_write';
  model?: string;             // 'claude-sonnet-4-6', 'voyage-3', etc.
  units: number;              // tokens, queries, bytes
  unitCost: number;           // custo por unidade em USD
  totalCost: number;          // units * unitCost em USD
  storyId: string | null;
  sessionId: string | null;
  metadata: Record<string, unknown>;
  createdAt: string;
}
```

### Preços de referência (2026)
| Operação | Modelo | Custo |
|----------|--------|-------|
| Input tokens | claude-sonnet-4-6 | $3/1M tokens |
| Output tokens | claude-sonnet-4-6 | $15/1M tokens |
| Embeddings | voyage-3 | $0.06/1M tokens |
| Web search | EXA | $0.005/query |
| Supabase | free tier | $0 (até 500MB, 2GB bandwidth) |

### API Endpoints (Hono — `packages/core`)
```
POST /api/costs/events          # Registra evento de custo
GET  /api/costs/summary?period=daily&date=2026-04-11  # Resumo agregado
GET  /api/costs/by-agent?period=monthly&month=2026-04  # Por agente
GET  /api/costs/budget          # Orçamento atual vs. gasto
```

### Schema Postgres
```sql
CREATE TABLE cost_events (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  agent_id TEXT NOT NULL,
  operation_type TEXT NOT NULL,
  model TEXT,
  units NUMERIC NOT NULL,
  unit_cost NUMERIC NOT NULL,
  total_cost NUMERIC NOT NULL,
  story_id TEXT,
  session_id TEXT,
  metadata JSONB DEFAULT '{}',
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX ON cost_events(agent_id, created_at DESC);
CREATE INDEX ON cost_events(created_at DESC);
```

### Orçamento atual
- Claude API: ~$550/mês (já pago)
- Meta: operações de pesquisa < $20/mês adicionais
- Threshold de alerta: se custo projetado no mês ultrapassar 90% do orçamento → escalação para Mauro (Story 1.6)

## Definition of Done
- [x] Todos os ACs implementados
- [x] `packages/core` com CostTracking service e endpoints funcionais
- [x] Testes Vitest cobrindo: registrar evento, agregar por período, agregar por agente
- [x] SOP de revisão de custos criado em `docs/sops/sop-gestao-custos.md`
- [x] Evento de custo registrado via testes (mocked end-to-end)
- [x] `npm run test` passando (28/28) — lint não instalado localmente (comportamento conhecido)
- [x] @qa PASS formal antes de marcar Done

## SOP
**SOP-1.8-A: Como revisar e otimizar custos no SparkleOS**
- Localização: `docs/sops/sop-gestao-custos.md` (criar nesta story)
- Conteúdo mínimo: (1) como verificar custo do período atual; (2) como identificar operações mais caras; (3) otimizações padrão (prompt caching, batch de embeddings); (4) threshold de alerta e ação quando atingido; (5) como registrar custo de nova operação ao adicionar funcionalidade

## File List
- `packages/core/src/costs/types.ts` — CostEvent, RecordCostInput, CostSummary, AgentCostSummary, BudgetStatus
- `packages/core/src/costs/cost-store.ts` — recordCost, getCostSummary, getCostByAgent, getBudgetStatus
- `packages/core/src/costs/cost-router.ts` — Hono router: POST /events, GET /summary, GET /by-agent, GET /budget
- `packages/core/src/costs/cost-store.test.ts` — 6 testes Vitest
- `packages/core/src/db/schema.ts` — costEvents table adicionada
- `packages/core/src/index.ts` — costRouter registrado em /api/costs
- `packages/core/migrations/0004_cost_events.sql` — migração SQL
- `docs/sops/sop-gestao-custos.md` — SOP-1.8-A completo
- `docs/sops/README.md` — atualizado com nova entrada

## QA Results

**Gate:** PASS  
**Revisado por:** @qa (Quinn) — 2026-04-11  
**Gate file:** `docs/qa/gates/1.8-rastreamento-custo.yml`

28/28 testes passando. Schema com NUMERIC para precisão financeira. Fire-and-forget documentado
no SOP. Índices para queries de agregação por período e agente. Budget status com threshold 90%.

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- `MONTHLY_BUDGET_USD` configurável via env (default: 570)
- `numeric` Drizzle type para evitar ponto flutuante em valores monetários
- `getCostSummary` e `getCostByAgent` usam mesma função `buildDateRange` — sem duplicação
- `getBudgetStatus` usa `sql` template literal para SUM agregado direto no Postgres
- Preços de referência 2026 documentados no SOP e nos Notas para @dev

### Change Log
- 2026-04-11: Story implementada por @dev (Dex) — CostTracking service completo

## Notas para @dev
- CostTracking deve ser leve — operações de registro não devem impactar performance do fluxo principal
- Usar `async fire-and-forget` para registrar custo — nunca bloquear fluxo principal por falha de custo
- Preços devem ser configuráveis via variável de ambiente ou config file (não hardcoded)
- Budget mensal configurável: `MONTHLY_BUDGET_USD=570` (referência: $550 Claude + $20 extras)
- Anthropic SDK emite token usage em cada chamada — capturar no response handler
