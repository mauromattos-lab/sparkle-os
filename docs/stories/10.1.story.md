---
story_id: "10.1"
title: "Auth de Clientes — Supabase Auth + sessão por tenant"
epic: "Epic 10 — Cockpit do Cliente Zenya"
status: "Draft"
created_by: "@sm (River)"
created_at: "2026-04-20"
assigned_to: "@data-engineer"
priority: "P1 — Blocker para 10.2, 10.3 e 10.4"
executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: [schema_validation, rls_test, auth_flow_test]
---

# Story 10.1 — Auth de Clientes — Supabase Auth + sessão por tenant

## User Story
Como cliente Zenya, quero fazer login no cockpit com meu e-mail, para que eu acesse exclusivamente os dados do meu negócio sem precisar de suporte do Mauro.

## Status
- [x] Draft
- [x] Ready (GO — @po Pax, 2026-04-20)
- [x] InProgress
- [ ] InReview
- [ ] Done

## Acceptance Criteria
- [x] 1. Supabase Auth configurado no projeto com magic link habilitado
- [x] 2. Tabela `zenya_client_users` criada vinculando `auth.users` a `zenya_tenants` via `tenant_id`
- [x] 3. RLS na tabela `zenya_client_users` garante que cada usuário vê apenas o próprio registro
- [x] 4. Ao fazer login, a sessão retorna `tenantId` e `tenantName` do tenant correspondente
- [x] 5. Usuário sem vínculo recebe `ClientNotFoundError` com mensagem clara
- [ ] 6. Regra Sparkle-first: fluxo testado com usuário real na instância SparkleOS — BLOQUEADO por Story 10.3

## Escopo

**IN (esta story entrega):**
- Configuração do Supabase Auth (email + magic link)
- Migration: tabela `zenya_client_users` com RLS
- Helper TypeScript: `getClientSession(userId)` → retorna `{ clientId, isolationKey }`
- Testes de isolamento: usuário A não pode acessar dados do cliente B

**OUT (não inclui):**
- UI de login (→ Story 10.3)
- Endpoints de API (→ Story 10.2)
- Convite/cadastro de novos usuários (→ Epic 14)

## Complexidade
**M (Médio)** — Schema e RLS bem definidos pelo padrão existente. Supabase Auth é novo mas bem documentado.

## Dev Notes

**Schema a criar:**
```sql
-- Vincula usuários auth.users a clientes Zenya
CREATE TABLE zenya_client_users (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id     UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  client_id   UUID NOT NULL REFERENCES zenya_clients(id) ON DELETE CASCADE,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE(user_id, client_id)
);

ALTER TABLE zenya_client_users ENABLE ROW LEVEL SECURITY;

CREATE POLICY client_user_self_only ON zenya_client_users
  FOR ALL TO authenticated
  USING (user_id = auth.uid());
```

**Helper de sessão:**
```typescript
// organs/zenya/src/auth/session.ts
export async function getClientSession(userId: string) {
  const row = await db.query(
    `SELECT zc.id, zc.data_isolation_key
     FROM zenya_client_users zcu
     JOIN zenya_clients zc ON zc.id = zcu.client_id
     WHERE zcu.user_id = $1 AND zc.status = 'active'
     LIMIT 1`,
    [userId]
  );
  if (!row.rows[0]) throw new Error('CLIENT_NOT_FOUND');
  return { clientId: row.rows[0].id, isolationKey: row.rows[0].data_isolation_key };
}
```

**Padrão de isolamento existente:** [Source: docs/zenya/ISOLATION-SPEC.md]
- `isolation_key` = `data_isolation_key` do `zenya_clients`
- Sempre usar `set_config('app.current_client_key', isolationKey, TRUE)` antes de queries

**Supabase Auth:** Email templates configurados em `Settings → Auth → Email Templates` no dashboard Supabase projeto `uqpwmygaktkgbknhmknx`.

## Tasks / Subtasks
- [x] 1. Configurar Supabase Auth: habilitar email + magic link no projeto `uqpwmygaktkgbknhmknx` (AC: 1) — manual via dashboard
- [x] 2. Criar migration `0003_zenya_client_users.sql` com tabela + RLS (AC: 2, 3)
- [x] 3. Migration aplicada via Management API VPS — tabela criada em produção, RLS ativo (AC: 2)
- [x] 4. Implementar `getClientSession()` em `organs/zenya/src/auth/session.ts` (AC: 4)
- [x] 5. Implementar erro `ClientNotFoundError` com mensagem clara (AC: 5)
- [x] 6. Escrever testes de isolamento: 5/5 passando (AC: 3, 6)
- [ ] 7. Teste end-to-end com usuário real na instância SparkleOS (AC: 6) — BLOQUEADO por Story 10.3 (UI de login ainda não existe). Magic link habilitado via API ✅. Executar após 10.3 Done.
- [x] 8. Quality gate manual (CodeRabbit não instalado no WSL): sem issues CRITICAL/HIGH — SQL seguro, RLS correto, FKs com CASCADE, índices presentes, idempotência garantida

## Riscos
- Supabase Auth free tier tem limite de 50k MAU — suficiente para fase inicial
- `auth.users` é gerenciado pelo Supabase — não fazer FK direta sem `ON DELETE CASCADE`

## 🤖 CodeRabbit Integration

**Story Type:** Database + Security
**Complexity:** Medium

**Agents:** @data-engineer (primary), @dev (quality gate)

**Quality Gates:**
- [ ] Pre-Commit (@dev): `wsl bash -c 'cd /mnt/c/Users/Mauro/Desktop/SparkleOS && ~/.local/bin/coderabbit --prompt-only -t uncommitted'`
- [ ] Pre-PR (@devops): `wsl bash -c 'cd /mnt/c/Users/Mauro/Desktop/SparkleOS && ~/.local/bin/coderabbit --prompt-only --base main'`

**Focus:** RLS policies, auth.users FK safety, isolation_key pattern consistency

## Change Log
| Data | Agente | Mudança |
|------|--------|---------|
| 2026-04-20 | @sm River | Story criada — Draft |
| 2026-04-20 | @data-engineer Dara | Migration aplicada via Management API da VPS. zenya_client_users criada em produção (rls_enabled=true, 1 policy). Descoberta: schema real usa zenya_tenants (não zenya_clients). session.ts adaptado. 5/5 testes passando. |
| 2026-04-20 | @dev Dex | Magic link habilitado via API. site_url=cockpit.sparkleai.tech. Story revertida para InProgress — AC 6 não cumprido (teste e2e real bloqueado por Story 10.3). Processo corrigido: @qa gate pendente. |
