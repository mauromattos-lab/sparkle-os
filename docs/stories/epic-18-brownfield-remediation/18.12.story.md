# Story 18.12 — Crypto helper em seed-common

**Epic:** 18 — Brownfield Remediation Zenya
**Wave:** 2 (Robustez)
**Status:** Draft
**TD:** TD-14 (P1)
**Esforço:** S (2-3h)
**Owner principal:** `@dev` Dex

## Story

**As** operador adicionando credencial nova de tenant,
**I want** `seed-common.mjs` exporte `encryptCredentialHex(plaintext, masterKey): string`,
**so that** scripts de seed (`seed-hl-ultracash.mjs`, futuros) não dupliquem lógica de criptografia AES-256-GCM.

## Acceptance Criteria

1. `seed-common.mjs` ganha `encryptCredentialHex(plaintext, masterKey)` que retorna `\\x{hex}` direto pronto pra UPSERT em `zenya_tenant_credentials`
2. `seed-hl-ultracash.mjs` refatorado pra usar helper (remove cópia local de `encryptCredential`)
3. Scripts futuros de seed-cred usam helper
4. Test em `__tests__/seed-common.test.ts` valida helper (encrypt + decrypt round-trip)

## Tasks

- [ ] Adicionar `encryptCredentialHex` em `seed-common.mjs`
- [ ] Refactor `seed-hl-ultracash.mjs`
- [ ] Adicionar test
- [ ] Cap. 4 §2.1 atualizado (referência atualizada)

## Dev Notes

DB Specialist Review §8 (esforço S, mecânico).

## Definition of Done

- [ ] Helper exportado
- [ ] `seed-hl-ultracash.mjs` usando helper
- [ ] Test passa
- [ ] QA gate PASS

---

*Story 18.12 — Epic 18 Wave 2 — 2026-04-26.*
