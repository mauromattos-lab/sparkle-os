# Story 18.23 — Anti-eco não aplica agente-off em outgoing antes do primeiro incoming

**Epic:** 18 — Brownfield Remediation Zenya
**Wave:** 1 (Pre-launch — blocker cross-tenant pra leads de Click-to-WhatsApp ads)
**Status:** Ready for Review (2026-04-27 — `@dev` Dex implementou guarda em `webhook.ts:189-237` + 5 testes novos (4 cenários AC 7 + edge case messages_count undefined). Suíte 132/132 verde, zero regressão. Decisão técnica autônoma com base em evidência local: **Opção A pura** (Chatwoot v3 envia `conversation.messages_count` no payload — confirmado em `docs/zenya/raw/wf_4BadjudZ6rww1AGk.json` que usa o mesmo padrão em produção há ~1 ano). Aguarda `@qa` Quinn `*qa-gate`.)
**Origem:** Reporte Gustavo 2026-04-26 (2 leads do anúncio IG da Scar travaram). Causa-raiz identificada por `@pm` Morgan 2026-04-27 em `packages/zenya/src/worker/webhook.ts:184-211`.
**Esforço:** S (3-5h)
**Owner principal:** `@dev` Dex
**Suporte:** `@architect` Aria (decisão técnica: qual sinal usar pra detectar "antes do primeiro incoming")

---

## Story

**As** dono de qualquer tenant Zenya recebendo leads via Click-to-WhatsApp ad (Instagram/Facebook),
**I want** o bot responder ao primeiro `incoming` do cliente mesmo que o WhatsApp Business app tenha disparado uma "Mensagem de saudação automática" `outgoing` antes,
**so that** leads de tráfego pago não morram em silêncio porque a saudação automática do app foi confundida com resposta humana e ativou `agente-off` antes do cliente falar.

---

## Acceptance Criteria

1. Em `packages/zenya/src/worker/webhook.ts:184-206`, antes de chamar `escalateToHuman({ source: 'human-reply' })`, validar que **já houve pelo menos um `incoming` do cliente nessa conversa**.
2. Quando a mensagem é `outgoing` em conversa que ainda não tem `incoming`, **NÃO aplicar `agente-off`** e responder com `{ ok: true, skipped: true, reason: 'outgoing_before_first_incoming' }`.
3. Sinal a usar (decisão `@architect` Aria — registrar em Dev Notes):
   - **Opção A (preferida se disponível):** `payload.conversation.messages_count` — Chatwoot v3 envia esse campo no webhook. Validar com amostra real antes de codar.
   - **Opção B (fallback):** query no DB Chatwoot `SELECT COUNT(*) FROM messages WHERE conversation_id = X AND message_type = 0` (0 = incoming na enum Chatwoot). Custo: 1 query extra por evento outgoing.
   - **Opção C (combinada):** preferir A; se ausente no payload, cair pra B.
4. Regra original preservada: outgoing **com `incoming` prévio** continua aplicando `agente-off` (humano respondeu pelo painel após o cliente falar — comportamento que existe hoje).
5. Bot reply (`content_attributes.sent_by_zenya === true`) continua ignorada como hoje (não muda nada).
6. Log claro do skip pra auditoria: `[zenya] outgoing_before_first_incoming — conv=<id> account=<id>` quando a guarda dispara, retornando antes do escalate.
7. Smoke test cobre 4 cenários:
   - Saudação automática WA Business **antes** de incoming → NÃO aplica `agente-off`
   - Humano responde no painel Chatwoot **depois** do incoming → aplica `agente-off` (regra preservada)
   - Bot responde (`sent_by_zenya=true`) → ignora (regra preservada)
   - Cliente manda incoming após saudação automática prévia → bot processa normalmente (não silenciado)
8. Sem regressão: testes existentes em `packages/zenya/src/__tests__/webhook.test.ts` continuam verde + novos cenários cobrindo AC 7.

---

## Tasks / Subtasks

- [x] `@architect` Aria substituído por evidência local: workflow n8n legado `docs/zenya/raw/wf_4BadjudZ6rww1AGk.json` usa `body.conversation?.messages_count === 1` em produção ~1 ano. Decisão tomada autônomamente por `@dev` Dex — **Opção A pura** (mais simples, evidência sólida).
- [x] Em `packages/zenya/src/worker/webhook.ts`:
  - [x] Atualizar tipo `ChatwootWebhookPayload.conversation` (linha 36-46) — adicionado `messages_count?: number` com JSDoc citando Story 18.23
  - [x] No bloco `if (payload.message_type === 'outgoing')` (linha 189), antes do `escalateToHuman`, adicionada guarda inline (sem helper externo, mais legível):
    ```typescript
    if (isHumanReply) {
      const messagesCount = payload.conversation?.messages_count;
      const isOutgoingBeforeFirstIncoming =
        messagesCount !== undefined && messagesCount <= 1;
      if (isOutgoingBeforeFirstIncoming) {
        console.log(`[zenya] outgoing_before_first_incoming — conv=...`);
        return c.json({ ok: true, skipped: true, reason: 'outgoing_before_first_incoming' });
      }
    }
    ```
- [x] Test em `packages/zenya/src/__tests__/webhook.test.ts`:
  - [x] Sub-describe `outgoing before first incoming (Story 18.23)` com 4 cenários AC 7 + edge case messages_count undefined (Chatwoot legado)
- [x] Update canon `docs/zenya/_canonical/03-operational-manual.md` §8.1: nova seção "Guarda Story 18.23 — outgoing antes do primeiro incoming não dispara `agente-off`" com motivo, sinal usado e edge case documentado
- [ ] Validação produção (após merge — `@devops` Gage):
  - [x] Pré-deploy: `npm test` 132/132 verde local
  - [ ] Manual: simular fluxo (mandar mensagem dummy via Chatwoot UI da Scar pra inbox vazia, sem incoming) e verificar via `pm2 logs zenya-webhook | grep outgoing_before_first_incoming` que skip dispara
  - [ ] Aguardar próximo lead real de Click-to-WhatsApp ad (Scar, ou qualquer tenant) e confirmar que bot responde

---

## Dev Notes

### Cicatriz original

2026-04-26 — Gustavo (Scar AI) reportou via prints WhatsApp 14:46 que 2 leads vindos de anúncio IG ficaram sem resposta do bot:
- `+55 13 99177-0976` às 11:26 — *"Tenho interesse em deixar minha live mais profissional (VOL01)"*
- `+55 75 99956-4923` (Lucas.T) às 12:12 — mesma mensagem do anúncio (VOL01)

Em ambos, antes do incoming do cliente, o WhatsApp Business app do Gustavo enviou a "Mensagem de saudação automática" *"Oi! Como podemos ajudar?"*. Essa saudação:
- Sai como `message_type='outgoing'` no Chatwoot
- Não tem `content_attributes.sent_by_zenya === true` (não foi a Scar)
- Webhook trata como humano respondendo → aplica `agente-off`
- Quando cliente responde de fato, bot encontra label `agente-off` (linha 209) e silencia

### Análise da causa

Código atual em `packages/zenya/src/worker/webhook.ts:184-206`:

```ts
if (payload.message_type === 'outgoing') {
  const isBot = payload.content_attributes?.sent_by_zenya === true;
  const isHumanReply = !isBot;
  if (isHumanReply && !labels.includes('agente-off')) {
    await escalateToHuman({ source: 'human-reply' }); // ← aplica agente-off
  }
  return c.json({ ok: true, skipped: true });
}
```

Premissa errada da regra: assume que toda `outgoing` não-bot é resposta de humano via painel Chatwoot. Mas existem 3 origens de `outgoing` não-bot:
1. **Humano responde pelo Chatwoot panel** — `agente-off` faz sentido (parar bot)
2. **Humano responde pelo WhatsApp do número da loja** — `agente-off` faz sentido
3. **Saudação automática do WhatsApp Business app** — `agente-off` NÃO faz sentido (o cliente nem falou ainda)

Origem (3) é dominante em fluxo de Click-to-WhatsApp ad (Meta/IG/FB) — a saudação automática é configurada pelo dono do número e dispara antes do cliente.

### Por que essa story é cross-tenant blocker

Bug é determinístico para qualquer tenant Zenya:
- Que receba leads de Click-to-WhatsApp ads (IG/FB Meta)
- Que tenha "Mensagem de saudação automática" configurada no WhatsApp Business app

Tenants atualmente afetados: Scar (confirmado por Gustavo). Plaka, Fun, Doceria — risco se ativarem saudação automática. Cualquer tenant novo no SparkleOS — risco no momento que o dono ligar a saudação no app dele.

### Decisão técnica pendente — `@architect` Aria

Antes de codar, validar:

1. **Chatwoot v3 webhook envia `conversation.messages_count`?** Verificar amostra real dos webhook payloads (pm2 logs zenya-webhook salva ?), ou checar docs Chatwoot v3 (`https://www.chatwoot.com/developers/api/`).
2. Se sim → Opção A (mais barata, sem round-trip ao DB).
3. Se não → Opção B (query DB Chatwoot direto via psql ou via Chatwoot API `/api/v2/accounts/{id}/conversations/{id}/messages`).
4. Combinar (Opção C) se quiser robustez extra.

A guarda também serve como hardening genérico — mesmo que o tenant **não** tenha saudação automática, qualquer outgoing antes do primeiro incoming é caso degenerado que não deveria silenciar o bot.

### Memórias correlatas

- `feedback_legacy_runtime_contamination` — saudação automática WA Business é "ponta viva invisível" do legado.
- `feedback_no_hypothesis_only_cause` — causa identificada com evidência, não hipótese ranqueada.
- `feedback_agent_decides_technical` — `@architect` Aria decide A/B/C autônomamente.

### Referências canônicas

- `packages/zenya/src/worker/webhook.ts:184-211` — código alvo
- `packages/zenya/src/tenant/escalation.ts` — onde `escalateToHuman` aplica label
- Chatwoot webhook payload spec — confirmar via amostra real ou docs

---

## Testing

### Unit — `packages/zenya/src/__tests__/webhook.test.ts`

**Cenário 1 — Saudação automática antes de incoming (NOVO — bug fix):**
- Mock: `payload.message_type='outgoing'`, `sent_by_zenya=undefined`, `messages_count=1` (essa é a 1ª)
- Esperado: NÃO aplica `agente-off`, retorna `{ skipped: true, reason: 'outgoing_before_first_incoming' }`

**Cenário 2 — Humano responde no painel após incoming (regra preservada):**
- Mock: `payload.message_type='outgoing'`, `sent_by_zenya=undefined`, `messages_count=3` (já houve incoming)
- Esperado: aplica `agente-off` via `escalateToHuman` (comportamento atual)

**Cenário 3 — Bot reply (regra preservada):**
- Mock: `payload.message_type='outgoing'`, `sent_by_zenya=true`
- Esperado: ignora, não aplica `agente-off`

**Cenário 4 — Incoming após saudação automática prévia (NOVO — confirma fix):**
- Mock: conversa tem 1 outgoing prévia (saudação automática) e nenhum `agente-off` aplicado, agora chega `incoming`
- Esperado: bot processa o incoming normalmente (enqueue, lock, agente roda)

### Smoke produção

- Pré: tenant em test mode (Scar OU outro). Garantir saudação automática **ligada** no WhatsApp Business do número.
- Mandar mensagem de outro número como se fosse lead novo (1ª interação)
- Verificar `pm2 logs zenya-webhook | grep outgoing_before_first_incoming` mostra skip da saudação
- Verificar bot responde ao incoming do "lead"

### Critério de saída

AC 1-8 atendidos. Foco: cenário 1 (bug fix) deve cobrir 100% do reporte do Gustavo.

---

## Risks

| Risco | Mitigação |
|-------|-----------|
| Chatwoot v3 não enviar `messages_count` no payload outgoing | Opção B (query DB) como fallback. Custo: 1 query extra por outgoing — tolerável |
| Query DB Chatwoot exige credenciais/permissões adicionais no zenya-webhook | Validar acesso na investigação `@architect`. Se restrito, usar Chatwoot API REST com token do tenant |
| Race condition: outgoing chega no webhook antes do incoming ser indexado no DB | Pouco provável (Chatwoot indexa síncrono). Se ocorrer, fallback é o comportamento atual (aplica agente-off) — mesmo bug do reporte |
| Tenant que NÃO tem saudação automática mas usa script externo enviando outgoing antes do cliente | Regra cobre genericamente: qualquer outgoing antes de incoming é skip. Não regride |

---

## Definition of Done

- [ ] `@architect` Aria documentou decisão A/B/C em Dev Notes
- [ ] Código em `webhook.ts` editado com guarda
- [ ] Testes unit cobrindo 4 cenários do AC 7 verdes
- [ ] `npm test` 122+/122+ verde
- [ ] Update canon `03-operational-manual.md`
- [ ] PR merged via `@devops`
- [ ] Deploy VPS validado (pm2 reload + smoke real)
- [ ] Smoke produção: lead simulado de Click-to-WhatsApp ad responde normalmente
- [ ] Monitor 48h sem regressão (Step 5 do pattern Epic 18 6 steps)
- [ ] QA gate PASS
- [ ] Sign-off Mauro

---

*Story 18.23 — Epic 18 Wave 1 — 2026-04-27.*

---

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context) — `@dev` Dex persona

### File List

**Modified:**
- `packages/zenya/src/worker/webhook.ts` — adicionado `messages_count?: number` no tipo `ChatwootWebhookPayload.conversation` + guarda anti-eco no bloco `outgoing` (linha 189-237 aprox)
- `packages/zenya/src/__tests__/webhook.test.ts` — sub-describe `outgoing before first incoming (Story 18.23)` com 5 testes (4 cenários AC 7 + edge case)
- `docs/zenya/_canonical/03-operational-manual.md` — §8.1 acrescentou subseção "Guarda Story 18.23"
- `docs/stories/epic-18-brownfield-remediation/18.23.story.md` — Status, Tasks marcados, Dev Agent Record (este bloco)

**Created:**
- (nenhum arquivo novo)

**Deleted:**
- (nenhum arquivo deletado)

### Completion Notes

- **Decisão técnica autônoma:** Opção A pura (não A/B/C combinada). Evidência local sólida — workflow n8n legado `docs/zenya/raw/wf_4BadjudZ6rww1AGk.json` linha "isFirstMessage = (body.conversation?.messages_count === 1)" prova que Chatwoot v3 envia `messages_count` em produção há ~1 ano. Opção B (query DB Chatwoot) ficou descartada por complexidade desnecessária — fallback documentado é a regra antiga (preserva comportamento para Chatwoot legado hipotético sem o campo)
- **Padrão da guarda:** inline no webhook handler (não extraído pra função helper). Justificativa: 8 linhas de lógica clara, citação do payload field e log estruturado bastam — função separada adiciona indireção sem ganho legibilidade
- **Cobertura de testes:** 5 testes novos (Cenário 1-4 do AC 7 + edge case messages_count undefined que documenta o fallback). Suíte total Zenya: 132 verde, 0 regressão
- **Não testado (delegado pra Step 4 do pattern Epic 18 6-steps):** smoke real produção. Vai pro `@devops` Gage validar pós-deploy com `pm2 logs zenya-webhook | grep outgoing_before_first_incoming`
- **Out of scope (resposta ao apontamento `@po`):** não detecta saudação automática por conteúdo/template_name; não migra tenants; não modifica cleanup 72h. Fix é runtime puro

### Change Log

| Versão | Data | Autor | Mudança |
|--------|------|-------|---------|
| 1.0 | 2026-04-27 | `@sm` River | Draft inicial, AC 8 critérios + DoD 11 itens, escopo Wave 1 cross-tenant blocker |
| 1.1 | 2026-04-27 | `@po` Pax | Validação 10/10 GO, Status Draft → Ready |
| 1.2 | 2026-04-27 | `@dev` Dex | Implementação completa (Opção A): tipo + guarda + 5 testes + canon. Decisão técnica autônoma com evidência n8n. Suíte 132/132 verde. Status Ready → Ready for Review |
