# Story 18.4 — KB sync ativo (worker PM2 dedicado)

**Epic:** 18 — Brownfield Remediation Zenya
**Wave:** 1 (Pre-launch)
**Status:** Ready for Review (implementation by @dev Dex 2026-04-26 — build OK + 122/122 tests sem regressão; smoke local --once exibe fluxo do código mas falha em fetch local por env Windows, smoke real prod obrigatório pós-deploy)
**TD:** TD-01 (P0)
**Esforço:** M (1 dia)
**Owner principal:** `@dev` Dex
**Suporte:** `@data-engineer` Dara (validação `last_synced_at`)

---

## Story

**As** dono da PLAKA (Roberta) e tenants futuros que usem Sheets KB,
**I want** o worker `kb-sync` rode automaticamente a cada 15 minutos sincronizando a planilha pro `zenya_tenant_kb_entries`,
**so that** mudanças na planilha cheguem ao bot em até 15 minutos sem intervenção manual.

---

## Acceptance Criteria

1. Novo script `packages/zenya/scripts/run-kb-sync.mjs` criado:
   - Default: loop infinito chamando `runKbSyncOnce()` a cada 15 min
   - Flag `--once` pra debug (1 sync e sai)
   - Flag `--tenant=X` pra debug isolado
2. `packages/zenya/ecosystem.config.cjs` atualizado:
   - Novo app `zenya-kb-sync` apontando pra `./scripts/run-kb-sync.mjs`
   - `autorestart: true`, `max_restarts: 5`, `restart_delay: 60000` (1 min entre restarts — não-urgente)
   - Logs separados em `./logs/kb-sync-*.log`
   - `max_memory_restart: 256M`
3. Após deploy, `pm2 list` mostra `zenya-kb-sync` online
4. Após 30min, `last_synced_at` da PLAKA atualiza (se planilha mudou) ou se mantém recente (se não mudou)
5. Logs `pm2 logs zenya-kb-sync --lines 20` mostram ciclos de sync acontecendo a cada 15min
6. Sem regressão: `zenya-webhook` continua funcionando normal (apps são independentes)

---

## Escopo — IN

- `packages/zenya/scripts/run-kb-sync.mjs` (novo): loop infinito + flags `--once`, `--tenant=X`
- `packages/zenya/ecosystem.config.cjs` (edit): adicionar 2º app `zenya-kb-sync` com autorestart + max_memory 256M + restart_delay 60s
- `docs/zenya/_canonical/03-operational-manual.md` Cap. 3 §11: nova app PM2 documentada
- `docs/zenya/_canonical/04-access-credentials.md`: confirmar `PLAKA_SHEETS_SA_PATH` documentada (sem mudança, validar)

## Escopo — OUT

- **Refactor de `kb-sync.ts` core** — só usa funções existentes (`startKbSyncLoop`, `runKbSyncOnce`, `syncTenantKB`); zero mudança no worker em si
- **Provisão Service Account novo Sheets** — assume `PLAKA_SHEETS_SA_PATH` já existe na VPS (validar não-criar)
- **KB sync de outros tenants além de PLAKA** — `syncTenantKB` itera sobre todos os tenants com Sheets configurado; story só ativa o loop, não adiciona/remove tenants
- **Endpoint admin pra forçar sync manual** — overkill; intervalo 15min + restart PM2 cobrem
- **Métricas/dashboard de sync** — escopo separado (Story 18.16 observabilidade Wave 3)
- **Webhook Sheets → core (real-time)** — sync ativo de 15min é suficiente; pull > push pra Sheets API rate-limit safety

## Tasks / Subtasks

- [x] Criar `packages/zenya/scripts/run-kb-sync.mjs`:
  - [x] Imports: `'dotenv/config'`, `startKbSyncLoop`, `runKbSyncOnce`, `syncTenantKB` from `../dist/worker/kb-sync.js`
  - [x] Parse args: `--once`, `--tenant=X`
  - [x] Lógica conforme spec na DB Specialist Review §2
- [x] Atualizar `packages/zenya/ecosystem.config.cjs`:
  - [x] Adicionar 2º app `zenya-kb-sync` no array
  - [x] Manter `zenya-webhook` intacto
- [x] Atualizar Cap. 3 §11.1 + nova §11.1.5 com nova app PM2 + comandos `pm2 logs zenya-kb-sync`, ad-hoc `--once`/`--tenant=`
- [~] **Cap. 4 dispensado:** Discovery durante implementação revelou que credenciais Sheets ficam em `zenya_tenant_credentials` (Tier 2 criptografado), não em env var `PLAKA_SHEETS_SA_PATH`. Task original tinha info stale do tempo n8n. Comentário adicionado em `run-kb-sync.mjs` documenta.
- [ ] Validação produção (escopo @devops Gage pós-merge):
  - [x] Pré-deploy: `npm run build` sucesso (validado local)
  - [ ] Deploy: `pm2 start ecosystem.config.cjs` ou `pm2 reload ecosystem.config.cjs --only zenya-kb-sync`
  - [ ] `pm2 list` mostra ambos apps online
  - [ ] `pm2 logs zenya-kb-sync --lines 50` confirma 1º sync
  - [ ] Query: `SELECT MAX(last_synced_at), NOW() - MAX(last_synced_at) AS age FROM zenya_tenant_kb_entries` → age <16min após 30min de wait

---

## Dev Notes

**Estado pré-fix:** Worker `kb-sync.ts` define `startKbSyncLoop()` mas NUNCA é invocado (`index.ts` só chama `startAgenteOffCleanup`). `ecosystem.config.cjs` tinha 1 app só. Resultado: sync único de 21/04 (provavelmente manual) — KB congelada por 4+ dias.

**Decisão técnica Dara:** worker PM2 separado (não dentro de `zenya-webhook`). Razão: isolation de falhas; KB sync pode crash em rate limit Sheets sem derrubar webhook.

**Referências canônicas:**
- DB Specialist Review §2 (proposta detalhada)
- Cap. 1 §4.9 (workers — TD-01 catalogada)
- Cap. 2 §6.6 (status atual KB sync)
- `packages/zenya/src/worker/kb-sync.ts` (código existente)

---

## Testing

**Smoke local:**
- `cd packages/zenya && node scripts/run-kb-sync.mjs --once` (com `.env` da VPS)
- Esperado: log `[kb-sync] sincronizando N tenants...` + `OK` por tenant
- Validação SQL: `SELECT MAX(last_synced_at) FROM zenya_tenant_kb_entries WHERE tenant_id = (SELECT id FROM zenya_tenants WHERE chatwoot_account_id='2')` → atualizou pra hoje

**Smoke produção:**
- `pm2 logs zenya-kb-sync --lines 100`
- Aguardar 30min e validar `last_synced_at` < 30min ago
- Se planilha PLAKA for mudada (Roberta/Isa), confirmar mudança aparece no banco em <16min

**Critério de saída:** AC 1-6 atendidos.

---

## Risks

| Risco | Mitigação |
|-------|-----------|
| Sheets API rate limit / Service Account expira | Worker tem `try/catch`; tolera falhas; `restart_delay: 60s` evita restart loop. Logs `error_file` documentam |
| KB sync sobrescreve mudanças manuais em `zenya_tenant_kb_entries` | KB é "snapshot"; fonte da verdade é Sheets. Mudanças manuais perdidas no próximo sync (intencional) |
| 2 apps PM2 confundem operação | Documentação Cap. 3 §11 deixa claro |

---

## Definition of Done

- [x] Script `run-kb-sync.mjs` criado
- [x] `ecosystem.config.cjs` atualizado (2 apps independentes)
- [x] Cap. 3 §11.1 + §11.1.5 atualizado
- [ ] PR merged via @devops Gage (próximo passo)
- [ ] `pm2 list` mostra ambos apps online em produção (Step 2 deploy)
- [ ] `last_synced_at` da PLAKA atualizando em ciclos de 15min (Step 4-5 smoke + monitor)
- [ ] QA gate PASS (próximo passo @qa Quinn)

---

## Dev Agent Record

**Agent Model Used:** Claude Opus 4.7 (1M context)

### Completion Notes

- Pre-flight identificou que credenciais `sheets_kb` ficam em `zenya_tenant_credentials` (Tier 2 criptografado), não em env var (`PLAKA_SHEETS_SA_PATH` era info stale do tempo n8n). Task 4 da story dispensada com nota documentada no `run-kb-sync.mjs`.
- `kb-sync.ts` worker já estava funcional (exporta `startKbSyncLoop`, `runKbSyncOnce`, `syncTenantKB`) — story só ativa via PM2 e adiciona runner script. Zero refactor do worker.
- 2 apps PM2 independentes: zenya-webhook (existente, max_memory 512M, restart_delay 3s) e zenya-kb-sync (novo, max_memory 256M, restart_delay 60s — Sheets rate limit safety).
- Smoke local --once mostrou erro "fetch failed" — ambiente Windows local sem acesso ao Supabase. Smoke real prod (Step 4) é obrigatório porque story tem side effect persistente (UPDATE em zenya_tenant_kb_entries).

### File List

| Arquivo | Tipo | Descrição |
|---------|------|-----------|
| `packages/zenya/scripts/run-kb-sync.mjs` | new | Runner com `--once`/`--tenant=X`/default loop infinito (PM2). Wrapper sobre `kb-sync.ts` |
| `packages/zenya/ecosystem.config.cjs` | edit | +2º app `zenya-kb-sync` (256M max, restart_delay 60s, logs separados em `./logs/kb-sync-*`) |
| `docs/zenya/_canonical/03-operational-manual.md` | edit | Cap. 3 §11.1 atualizado (deploy 2 apps) + §11.1.5 nova (operações específicas zenya-kb-sync com debug `--once`/`--tenant=`) |

---

## QA Results

### Gate Decision: ✅ **PASS** (Score 64/70 — threshold 49/70)

**Reviewer:** @qa Quinn — 2026-04-26
**Gate file:** [`docs/qa/gates/18.4-kb-sync-ativo.yaml`](../../qa/gates/18.4-kb-sync-ativo.yaml)
**Tipo:** Comprehensive (7-check + advisory)

### 7 Quality Checks

| # | Check | Score | Verdict |
|---|-------|-------|---------|
| 1 | Code Review | 10/10 | ✅ PASS — 2 apps independentes, params adequados, Dara isolation rationale documentada |
| 2 | Test Coverage | 7/10 | 🟡 CONCERNS — zero testes novos pra .mjs (justificado wrapper); smoke local não-completo (env Windows) |
| 3 | AC Traceability | 8/10 | ✅ PASS-WITH-PENDING — 3/6 ACs operacionais (esperado pra worker runtime) |
| 4 | No Regressions | 10/10 | ✅ PASS — 122/122 paridade |
| 5 | Performance | 10/10 | ✅ PASS — 15min interval + 60s restart_delay + 256M memory adequados |
| 6 | Security | 10/10 | ✅ PASS — credenciais Tier 2 criptografado; discovery removeu info stale |
| 7 | Documentation | 9/10 | ✅ PASS — Cap. 3 §11.1 + §11.1.5 publicadas |

### Concerns (não-bloqueantes)

- **MEDIUM (test-coverage):** smoke local fetch-failed (env Windows). Mitigação: Step 4 smoke prod obrigatório.
- **LOW (operational):** 2 apps PM2 podem confundir reaviso — Cap. 3 §11.1.5 cobre.

### Riscos identificados (mitigados)

| Risco | Probabilidade | Impacto | Mitigação |
|-------|---------------|---------|-----------|
| R1: ecosystem.config malformado quebra pm2 reload | LOW | MEDIUM | Devops valida via `pm2 reload ecosystem.config.cjs` no deploy |
| R2: Service Account expirar mid-sync | LOW | LOW | try/catch existente + restart_delay 60s |
| R3: Race kb-sync ↔ webhook em zenya_tenant_kb_entries | VERY LOW | LOW | UPSERT atomic Postgres; reads de coluna diferente |

### Smoke Step 4 — OBRIGATÓRIO

Story tem side effect persistente (UPDATE em `zenya_tenant_kb_entries`). Cobertura unit não substitui validação prod.

**Cenários:**
1. `pm2 list` mostra `zenya-kb-sync` online, restarts <5, memória <256M
2. Aguardar 30min, query `SELECT MAX(last_synced_at), NOW() - MAX(last_synced_at) AS age FROM zenya_tenant_kb_entries` → age <16min
3. `pm2 logs zenya-kb-sync --lines 50` mostra `[kb-sync] sincronizando N tenants... OK`
4. Monitor 24-48h: contagem de erros estável

### Conclusão Quinn

Implementação Dex sólida com discovery positivo (removeu info stale `PLAKA_SHEETS_SA_PATH`). Code, security e performance excelentes. Único gap: smoke local não-completo, mitigado pelo Step 4 obrigatório. **Aprovado pra @devops Gage push + deploy.**

---

*Story 18.4 — Epic 18 Wave 1 — 2026-04-26.*

---

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-04-26 (criação) | @pm Morgan (Phase 10 brownfield) | Story 18.4 draftada com 6 ACs + Tasks + DoD |
| 2026-04-26 (validation) | @po Pax | 10-point checklist GO 9.5/10. Status: Draft → Ready. Adicionada seção Escopo IN/OUT explícita (fix gap #4 da validation). Pronta pra `@dev *develop`. |
| 2026-04-26 (implementation) | @dev Dex | Pre-flight + implementação. Discovery: credenciais Sheets vão em `zenya_tenant_credentials` (não env). Build OK, 122/122 tests sem regressão. Smoke local fetch-failed (env Windows network — não-bloqueante; smoke real prod obrigatório). Status: Ready → Ready for Review. Pronta pra `@qa *review`. |
