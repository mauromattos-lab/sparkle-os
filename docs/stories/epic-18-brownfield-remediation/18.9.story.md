# Story 18.9 — Cache invalidação via NOTIFY/LISTEN

**Epic:** 18 — Brownfield Remediation Zenya
**Wave:** 2 (Robustez)
**Status:** Draft
**TD:** TD-11 (P1)
**Esforço:** S (4-8h)
**Owner principal:** `@data-engineer` Dara + `@dev` Dex

## Story

**As** operador atualizando prompt de tenant em produção,
**I want** mudança em `zenya_tenants` propague pra todas instâncias do `zenya-webhook` em <5s (não 5min do TTL),
**so that** ajustes de prompt fiquem visíveis ao bot quase imediatamente.

## Acceptance Criteria

1. Migration 012 cria function `notify_tenant_changed()` + trigger AFTER INSERT/UPDATE/DELETE em `zenya_tenants`
2. `config-loader.ts` ganha `subscribeTenantChanges()` chamado em `index.ts` startup
3. Quando `UPDATE zenya_tenants` acontece, todas instâncias `zenya-webhook` invalidam cache em <5s
4. Smoke: `UPDATE zenya_tenants SET system_prompt = ... WHERE chatwoot_account_id = '5'` → próxima mensagem pega prompt novo (não 5min)

## Tasks

- [ ] Migration 012: function + trigger
- [ ] Editar `config-loader.ts`: adicionar `subscribeTenantChanges()` usando Supabase realtime
- [ ] Editar `index.ts`: chamar `subscribeTenantChanges()` na inicialização
- [ ] Test: mock + verificar invalidação
- [ ] Cap. 4 atualizar

## Dev Notes

DB Specialist Review §7 — Postgres LISTEN/NOTIFY (não endpoint admin). Schema `realtime` já existe no Supabase.

## Definition of Done

- [ ] Migration 012 aplicada
- [ ] Code editado
- [ ] Smoke OK
- [ ] QA gate PASS

---

*Story 18.9 — Epic 18 Wave 2 — 2026-04-26.*
